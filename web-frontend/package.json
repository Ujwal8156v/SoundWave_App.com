{
  "name": "soundwave-web-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "devDependencies": {
    "vite": "^6.4.3"
  }
}
